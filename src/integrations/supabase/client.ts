// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';
import { supabaseWithProxy, directSupabase } from './proxyClient';

// Use environment variables only - no hardcoded credentials
const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

// Validate required environment variables
if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
  throw new Error('Missing required environment variables: VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY must be set');
}

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

// Use proxy-enabled client that automatically detects corporate firewalls
export const supabase = supabaseWithProxy;

// Export direct client for fallback if needed
export const supabaseDirect = directSupabase;

// Legacy export for backward compatibility
export const supabaseOriginal = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY);