# Supabase Proxy Configuration
# Set to 'true' to route all Supabase calls through your domain (for security compliance)
# Set to 'false' or leave empty for direct Supabase calls (development)
VITE_USE_SUPABASE_PROXY=false

# Example for production deployment:
# VITE_USE_SUPABASE_PROXY=true