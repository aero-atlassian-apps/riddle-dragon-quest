# Supabase Configuration
# CRITICAL: These must be set for the application to work
# DO NOT commit actual values to version control
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side Supabase Configuration (for proxy)
# This should match VITE_SUPABASE_ANON_KEY
SUPABASE_ANON_KEY=your-anon-key-here

# Proxy Configuration
# Set to 'true' to force all requests through proxy (useful for corporate networks)
VITE_FORCE_PROXY=false

# Proxy test timeout in milliseconds
VITE_PROXY_TEST_TIMEOUT=3000

# Security Notes:
# 1. Never commit actual credentials to version control
# 2. Use different keys for development and production
# 3. Regularly rotate your Supabase keys
# 4. Ensure SUPABASE_ANON_KEY is set in your deployment environment
# 5. The proxy endpoint validates requests to prevent SSRF attacks